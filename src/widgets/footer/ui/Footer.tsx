export const Footer = () => {
    return (
        <footer className="flex items-center justify-center">
            <p>
                &copy;takedelight.
                {new Date().getFullYear()} year.All
                rights reserved
            </p>
        </footer>
    );
};
